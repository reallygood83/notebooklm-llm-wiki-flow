# Quickstart

1. Clone the repository
2. Run `./scripts/bootstrap.sh`
3. Run `notebooklm login`
4. Run `nlwflow init-config`
5. Run `nlwflow doctor --json`
6. Run `nlwflow plan-policy-compare --json`
